# 产品规格书 v1.2 — DateRadar

> 本规格书供 Codex(或任何 AI 编程工具)直接参照开发。所有页面、流程、字段、接口均为 MVP 定义,按"小步验证"拆成里程碑。

---

## 0. 产品名

**DateRadar**(已定,2026-08-31)。
- 品牌标识:DATERADAR(全大写 logo)
- 域名建议:dateradar.com(未注册需尽快抢注;备选 dateradar.app / getdateradar.com)

---

## 1. 产品定位

**一句话**:面向美国约会用户的"关系风险雷达"——上传约会对话(聊天记录或音频),AI 按信号库分析,输出风险报告与行动建议。

**差异化**:不是"约会教练"(教你怎么聊),是**风险雷达**(这段关系有没有坑)。反诈定位有 FTC/FBI 官方数据背书。

**目标市场**:美国首发(英语单语)。底层信号库男女通用,同一套分析引擎,前端按用户视角切换措辞。

---

## 2. 目标用户

- 主人群:20–45 岁美国约会用户,男女通用
- 高价值细分:60+ 单身中老年(浪漫诈骗受害者集中,FBI 统计 60+ 占投诉 43%、损失 58%),反诈定位可吸引媒体/机构关注

---

## 3. 核心流程

```
用户打开网站
  → 选择分析方式:① 粘贴聊天记录 ② 上传聊天截图(多张) ③ 上传音频(录音 ≤10 分钟)
  → (截图模式)OCR 转文字;(音频模式)Whisper 转文字;音频/图片即删不存
  → AI 按信号库逐条对照对话文本
  → 输出报告(免费版:雷达图+风险等级+总评;付费版:证据链+话术+下次清单)
```

---

## 4. 页面清单(MVP)

| 页面 | 功能要点 |
|---|---|
| **首页 Landing** | 产品名 + 一句话定位 + FBI/FTC 反诈数据背书("FBI 统计浪漫诈骗年损失超 $6 亿") + CTA 按钮"分析一段对话" |
| **分析页 Analyze** | 三个入口:① 粘贴聊天记录 ② 上传聊天截图(多张,OCR 转文字,可编辑草稿) ③ 上传音频;录音上限 10 分钟;录音知情提示 |
| **免费报告页** | 风险等级徽章(低/中/高/🔴)+ 六类信号雷达图 + 一句话总评 + 免责声明;**刻意设计为一屏、美观、可截图**;底部 CTA 引导解锁完整报告 |
| **付费报告页**(解锁后) | 每个命中信号:原文证据引用(时间戳/原文片段) + 信号解读 + **应对话术建议** + **下次约会观察清单**;可生成**只读分享链接**(发给朋友一起参谋) |
| **定价/支付** | 单次 $4.99,打包 3 次 $9.99;冷启动期首次完整报告免费体验;MVP 用"开发模式"解锁,后期接 Paddle |
| **帮助/免责页** | TOS、隐私政策、免责声明(结果仅供参考,需多次综合判断)、通知删除/举报通道 |

---

## 5. 报告字段(固定 schema,保证可复现)

> AI 输出必须严格遵循以下 JSON 结构,同参数重跑结果一致。前端据此渲染。
> 注意:`timestamp_sec` 仅音频模式有值;文本/截图来源填 `null`。

```json
{
  "risk_level": "low | medium | high | critical",
  "summary": "一句话总评",
  "radar": {
    "avoidant": 0, "extractive": 0, "breadcrumbing": 0,
    "manipulative": 0, "deceptive": 0, "scam": 0
  },
  "signal_hits": [
    {
      "signal_id": "S14",
      "signal_name": "Love bombing then withdrawal",
      "matched_quote": "对方对话中的原文片段",
      "timestamp_sec": 123,
      "explanation": "为什么判定命中",
      "advice": "针对本条信号的应对话术"
    }
  ],
  "next_checklist": ["下次约会重点观察的 2-3 个行为点"],
  "disclaimers": "免责声明文案(固定模板)"
}
```

- 风险等级由信号库的**叠加规则**计算(见 signals-library.md)
- 命中 🔴(scam 类 ≥1)时,报告顶部显示红色预警横幅 + 防诈提示(含 60+ 受害者统计、不可逆支付警示)

**雷达图渲染规则**(radar 六个维度的显示逻辑):
- 角色:分维度风险快照,只回答"哪类信号有问题、有多严重";总体结论由风险等级徽章承担
- 三个元素:① 轴标签=类别名称 ② 顶点取值=该维度命中信号数量 ③ 颜色=该维度风险强度
- **颜色由数据决定,不按类别写死**(禁止"某类固定绿/红"):每条维度独立按下表分级
  | 该维度命中情况 | 颜色 |
  |---|---|
  | 0 条 | 绿(该维度干净,未发现异常) |
  | 1 条且非高危信号 | 黄(提示) |
  | 2 条,或 1 条高危信号(操控/索取类内的高危项) | 橙(警告) |
  | ≥3 条,或命中骗财(S23–S25) | 红/深红(危险) |
- **颜色呼应**:每个维度的顶点数据点颜色 = 该维度状态色(绿/黄/橙/红);连线/描边同色;图例与图上颜色一一对应
- **高亮预警(数据驱动触发)**:当某维度达到红色等级(≥3 条 或命中骗财)时,该维度触发高亮视觉。效果:骗财命中必然高亮(命中即红);其它类别高危时同样高亮(不按类别写死)。**高亮必须克制**:优先用"加粗顶点 + 该维度边缘线用危险色"强调;禁止大面积红色光晕/晕影、禁止多条红色虚线轴线(会形成"蜘蛛网"感);以数据多边形为视觉主体,中心区域保持干净(不放置标签/刻度数字)
- 六个顶点带类别文字标签,文字放顶点外侧,注意防溢出
- 图例文案统一:"颜色越深 = 该类别命中的危险信号越多"
- 首页示例图(Signal Snapshot)用示例数据展示读法:操控(橙)、骗财(红+高亮)、索取(黄)、其余三类(绿)

---

## 6. 定价与付费墙

| 层级 | 内容 | 门槛 |
|---|---|---|
| 免费版 | 风险等级徽章 + 雷达图 + 一句话总评(可截图传播) | 需登录(邮箱/Google)防滥用;或每日限 1 次 |
| 付费版 | 完整报告:证据链 + 解读 + 应对话术 + 下次清单 + 分享链接 | 单次 $4.99;打包 3 次 $9.99 |
| 冷启动 | 首次完整报告免费体验(引导注册) | 早期阶段 |

- **按次购买,不做订阅制**(低频工具,用户不会月月用)
- 单次分析成本预估 < $0.2(Whisper + Claude),毛利空间充足
- 收款实现:MVP 阶段用 `DEV_MODE=true` 跳过支付直接解锁;接入真实收款用 **Paddle**(MoR,代收代缴税,对国内开发者友好)优先,有海外主体再上 Stripe

---

## 7. 合规与隐私架构(必须实现)

> 这是产品的安全底线,实现优先级最高。中美两市场通用。

1. **音频/图片即删不存**:音频完成转文字、截图完成 OCR 后立即删除,服务器不保留原始音频与图片;无任何分享/公开功能。
2. **聊天记录模式**:支持粘贴文字对话分析,完全绕开录音同意问题;录音模式提示"请确保已获得对方知情同意"。
3. **措辞红线**:报告只报"命中信号 + 行为证据",**绝不输出"渣/渣女/渣男"之类标签或定罪结论**;附带固定免责声明。
4. **法律页面**:TOS(用户声明拥有音频/对话的使用权)、Privacy Policy、Disclaimer、举报/删除通道(通知-删除,满足"安全港")。
5. **不教唆**:所有产品文案不得鼓励偷录、不得贬低被分析对象。

---

## 8. 技术栈(Codex 参照)

| 环节 | 方案 | 理由 |
|---|---|---|
| 网站本体 | Next.js(App Router)+ Tailwind,部署 Vercel | Codex 最熟;Vercel 一键部署零运维 |
| 状态/用户 | 轻量方案:初期可不用数据库;需要时 Supabase(托管 Postgres) | 免运维,可视化 |
| 语音转文字 | **OpenAI Whisper API**;转完即删 | 音频模式 |
| 截图 OCR | **Tesseract.js(浏览器本地)**;免费、无 key、图片不出设备;OCR 结果作为可编辑草稿 | 聊天截图模式 |
| 分析引擎 | **Anthropic Claude API(默认)**;`AI_PROVIDER` 可切 **DeepSeek** | 分析质量 + 预留国内备选 |
| 录音 | 浏览器 MediaRecorder API | 纯前端实现 |
| 收款 | 开发模式解锁起步;后期 **Paddle**(MoR)或 Stripe | MoR 对国内开发者友好 |
| 订阅/订单 | 后期可选 Supabase 落库 | |

**架构要点**:
- `src/lib/ai/` 做 **provider 抽象**:`analyze(transcript)` 按 `AI_PROVIDER`(anthropic/deepseek)选择实现;**两个 provider 输出完全相同的 JSON schema**,切换零改动。这是给用户预留的 DeepSeek 口子。
- 分析是服务端调用,前端拿到固定 JSON 渲染报告。
- 信号库以 `signals.json` 注入分析提示词,AI 只做"逐条对照 → 结构化输出",不做自由发挥。
- 分析接口需做**限流**(登录门槛 + 免费次数限制 + 简单 IP 限流)。

---

## 9. AI 分析提示词结构(规格)

> 默认走 Claude(Anthropic SDK 结构化输出);切 DeepSeek 时走 `response_format: { "type": "json_object" }`。两者输出同一 schema。

```
System:
  你是一位约会安全分析助手。严格依据给定的信号库(signals.json)分析对话。
  规则:
  1. 逐条对照信号库,判断是否命中;命中必须给出原文证据(matched_quote + 时间戳)。
  2. 绝不输出"渣/渣女/渣男"等标签或定性结论,只报"命中行为信号"。
  3. 严格输出以下 JSON 结构(不可多字段、不可少字段):{risk_level, summary, radar, signal_hits[], next_checklist, disclaimers}
  4. risk_level 依据叠加规则计算:scam命中→critical;≥3条 或≥2操控/索取→high;2条→medium;1条→low。
  5. 不确定时不命中,宁可漏报(防止误伤)。

User: [对话转写文本]
```

---

## 10. MVP 里程碑(小步验证,每步可验收)

| 里程碑 | 内容 | 验收标准 |
|---|---|---|
| **M1 骨架** | Landing 页 + 分析页(粘贴聊天记录 + 上传音频入口) | 能粘贴文本/上传音频,展示"分析中"状态 |
| **M2 输入转换** | 接 Whisper(音频→文字)+ Tesseract.js(截图→文字,可编辑草稿);音频/图片即删 | 10 分钟音频 < 60 秒出文字;多张截图可上传;服务器不留存音频和图片 |
| **M3 分析+免费报告** | 接 AI 分析(Claude 默认,DeepSeek 可切)+ signals.json + 免费报告页(雷达图/等级/总评) | 测试对话输出固定 JSON 并渲染;同段对话重复分析结果一致 |
| **M4 付费墙** | 报告内容锁定 + 开发模式解锁(有收款账号再切 Paddle/Stripe) | 免费→付费解锁流程通,订单逻辑有记录 |
| **M5 合规+发布** | 免责/隐私/TOS 页、举报通道、录音知情提示、限流、部署 Vercel | 自查清单全部通过,可上线 |

**测试集**:用 codex-brief.md 第 9 节的三段测试对话(正常/中风险/杀猪盘)回归,保证不误伤正常对话。

---

## 11. 待办/开放问题

- [ ] 产品名已定 DateRadar;抢注 dateradar.com
- [ ] 完整 25 条信号的英文判据翻译、生成 signals.json(依据 signals-library.md)
- [ ] 三段测试对话已内置在 codex-brief.md
- [ ] 冷启动获客内容(免费雷达图的可截图模板、TikTok 传播素材)见 marketing-kit.md
- [ ] 注册 Anthropic + OpenAI 账号,拿到 key;DeepSeek key 可选预留
- [ ] 如打中国市场:单独补国内鉴渣博主研究 + 本地化
